
public class UIEnums
{
    public enum ElementType
    {
        Canvas, Panel, Button, Dropdown, InputField, Text,
        Image, Toggle, Slider, PopUp, Gauge, Video, Table
    }

    public enum ARTrackingTypes
    {
        ARTrackingFound, ARTrackingLost, ApplicationLoad, ApplicationExit
    }

    public enum UIEnums3
    {
        fadeIn3,
        fadeOut
    }

    public enum UIEnums4
    {
        fadeIn4,
        fadeOut
    }

    public enum AssetTypes
    {
        Audio, Video, ThreeDObject, Image,
        Document, Icon
    }

    public enum EventTypes
    {
        OnClick, ItemSelect, ValueChange, Check, Click, GotFocus,
        LostFocus, Close, Open, Show, Play, Pause, Stop
    }

}